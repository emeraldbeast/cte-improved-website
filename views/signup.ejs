<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 flex items-center justify-center">
        <div class="card text-white w-72 h-[520px] bg-zinc-800 border rounded-lg border-zinc-600 p-4">
            <div class="cte-logo items-center">
                <a href="/">
                    <img src="https://bpgc-cte.org/logo.png">
                </a>
            </div>
            <form action="/signup" method="post" class="flex flex-col items-center gap-4 mt-4">
                <input type="text" name="username" placeholder="Name" class="bg-transparent border border-zinc-500 outline-none rounded-lg px-4 py-2 w-full" >
                <input type="email" name="email" placeholder="Email" class="bg-transparent border border-zinc-500 outline-none rounded-lg px-4 py-2 w-full">
                <input type="password" name="password" placeholder="Password" class="bg-transparent border border-zinc-500 outline-none rounded-lg px-4 py-2 w-full">
                <input type="password" name="confirmPassword" placeholder="Confirm Password" class="bg-transparent border border-zinc-500 outline-none rounded-lg px-4 py-2 w-full">
                <input type="submit" value="Sign Up" class="bg-blue-500 text-white text-lg px-6 py-2 rounded hover:bg-blue-600 cursor-pointer">
                <% if (errorMessage) { %>
                    <p class="text-red-600 text-sm"><%= errorMessage %></p>
                <% } %>
            </form>
            <div class="login items-center text-center mt-3">
                <a href="/login" class="inline-block px-3 py-2 border border-zinc-500 rounded-lg text-white hover:bg-zinc-700 transition-all">
                    Already have an account?
                </a>
            </div>
        </div>
    </div>
</body>
</html>